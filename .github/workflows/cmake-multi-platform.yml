name: CMake CI

on:
  push:
    branches: ["main"]
  pull_request:
    branches: ["main"]

jobs:
  build:
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]

    runs-on: ${{ matrix.os }}

    steps:
      - uses: actions/checkout@v4

      - name: Install Boost (Linux)
        if: matrix.os == 'ubuntu-latest'
        run: |
          sudo apt-get update
          sudo apt-get install -y libboost-all-dev libpq-dev google-benchmark

      - name: Install Boost (macOS)
        if: matrix.os == 'macos-latest'
        run: |
          brew install boost libpq google-benchmark

      - name: Install Boost libraries (Windows)
        if: matrix.os == 'windows-latest'
        uses: MarkusJx/install-boost@v1.1.7
        with:
          boost_version: 1.83.0
          toolset: msvc
          cache: true
          libs: "all"

      - name: Cache CMake build
        uses: actions/cache@v4
        with:
          path: build
          key: ${{ runner.os }}-cmake-${{ hashFiles('**/CMakeLists.txt') }}
      
      - name: Cache vcpkg
        if: matrix.os == 'windows-latest'
        uses: actions/cache@v4
        with:
          path: |
            vcpkg/installed
            vcpkg/packages
          key: ${{ runner.os }}-vcpkg-${{ hashFiles('**/CMakeLists.txt') }}

      - name: Configure
        run: |
          cmake -B build -S . \
          -DCMAKE_BUILD_TYPE=Release \
          ${{ matrix.os == 'windows-latest' && '-DCMAKE_TOOLCHAIN_FILE=vcpkg/scripts/buildsystems/vcpkg.cmake' || '' }}

      - name: Build
        run: cmake --build build --config Release

      - name: Test
        working-directory: build
        run: ctest --output-on-failure